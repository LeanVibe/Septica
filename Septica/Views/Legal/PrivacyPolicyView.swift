import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View {
        ScrollView {
            Text("Privacy policy placeholder.")
                .padding()
        }
        .navigationTitle("Privacy Policy")
    }
}
