import SwiftUI

struct RulesView: View {
    var body: some View {
        ScrollView {
            Text("Septica rules coming soon.")
                .padding()
        }
        .navigationTitle("Rules")
    }
}
