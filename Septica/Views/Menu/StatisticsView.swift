import SwiftUI

struct StatisticsView: View {
    var body: some View {
        VStack(spacing: 8) {
            Text("Statistics")
                .font(.title)
            Text("No statistics available yet.")
        }
        .padding()
    }
}
